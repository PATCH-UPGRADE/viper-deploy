start:
    bash ../lib/start.sh

stop:
    bash ../lib/stop.sh

reset:
    bash ../lib/reset.sh

create-viper-api-key:
    bash ../lib/create-viper-api-key.sh

seed:
    bash init/seed.sh

scale:
    bash init/scale.sh

test:
    just create-viper-api-key
    just seed
    just scale
